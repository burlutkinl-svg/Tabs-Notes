Стек:
    python: fastapi, sqlalchemy, uvicorn, python-multipart, pydantic, aiofiles;
    база данных: SQLite

Структура бэкенда:

backend/                      # корень проекта
├── app/                      # основной пакет приложения
│   ├── api/                  # маршруты и обработчики запросов
│   ├── db/                   # настройка подключения к базе данных
│   ├── models/               # ORM-модели
│   ├── schemas/              # Pydantic-схемы валидации
│   ├── services/             # функции для работы бэка
├── uploads/                  # загружаемые пользователем файлы
├── main.py                   # точка входа
└── README.md                 # документация

На данном этапе ORM-модели не созданы, Pydantic-схемы не описаны, функции для работы демона с бэком не созданы.