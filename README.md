Для запуска бэкенда(cmd):

pip install -r requirements.txt
cd backend
uvicorn main:app --reload

Бэкенд работает по адресу http://127.0.0.1:8000

Для запуска фронтенда(cmd):

#Вместо этой строки описать процесс запуска фронтенда

**Описание**

На данном этапе создана структура бэкенда и описаны основные эндпоинты, возвращающие фолбэки. Документацию(не полную, не описаны ORM-модели) эндпоинтов можно посмотреть в Swagger-е от FastAPI по адресу http://127.0.0.1:8000/docs.

#При пуше описать в этот README(вместо этой строки) реализованный функционал для пользователя, то есть как пользователь его ощущает.